org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Fabric Properties
# Double-check these against https://fabricmc.net/develop/ if the build fails -
# these version numbers move fast and the exact patch build can change week to week.
minecraft_version=1.21.11
loader_version=0.19.3
loom_version=1.16-SNAPSHOT

# Mod Properties
mod_version=1.0.0
maven_group=com.example
archives_base_name=grapplinghook

# Dependencies
fabric_api_version=0.141.1+1.21.11
